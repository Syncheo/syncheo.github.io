{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
        xmlns:xhtml="http://www.w3.org/1999/xhtml">
  {{- /* On exclut les pages de taxonomie et de terme : elles sont en noindex,
         leur présence dans le sitemap ne ferait que générer des avertissements
         "URL envoyée marquée noindex" dans la Search Console. */ -}}
  {{- range site.Pages }}
  {{- if and .Permalink (not (in (slice "taxonomy" "term") .Kind)) }}
  <url>
    <loc>{{ .Permalink }}</loc>
    {{- if not .Lastmod.IsZero }}
    <lastmod>{{ .Lastmod.Format "2006-01-02T15:04:05-07:00" }}</lastmod>
    {{- end }}
    {{- if .IsTranslated }}
    {{- range .AllTranslations }}
    <xhtml:link rel="alternate" hreflang="{{ .Language.Lang }}" href="{{ .Permalink }}" />
    {{- end }}
    {{- end }}
  </url>
  {{- end }}
  {{- end }}
</urlset>
